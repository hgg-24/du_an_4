name: Keep ED-ODYSSEY Alive 4
on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Wake up the app
        run: curl -s https://thong-ke.streamlit.app/ > /dev/null
